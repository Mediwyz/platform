'use client'

import WyzoAssistant from '@/components/shared/WyzoAssistant'

/** Home "Discover" placement of the shared Wyzo agent (search + booking + Q&A). */
export default function DiscoverAiSearch() {
  return <WyzoAssistant variant="panel" />
}
